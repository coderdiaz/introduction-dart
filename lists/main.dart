void main() {
  List<int> numbers = [1, 2, 3, 4, 5];
  print(numbers);

  // Adding element to list
  numbers.add(6);
  print(numbers);

  // List with specific size
  List<int> listNumbers = List(10);
  print(listNumbers);
}